# S7 — Security — Money Transfer

> Summary of [`S7-security-money-transfer.json`](S7-security-money-transfer.json). The JSON remains the source of truth for envelope validation; this file makes the payload human-scannable.

## Envelope

| Field | Value |
|---|---|
| Artifact ID | `16792683-0871-4e95-826c-2711bf2a14fc` |
| From | `banking-security` |
| To | `banking-qa` |
| Phase | `SECURITY` |
| Feature | `money-transfer` |
| Timestamp | `2026-05-18T04:05:00Z` |
| Verdict | ✅ approved |
| Iteration | 1 |
| Quality Gate | ✅ Passed |
| Previous artifact | `9e2c4a8b-5f31-4d72-b3a1-7e6c0d9f4b21` |

## TL;DR

✅ **Approved** to QA with 0 critical, 0 high, 3 medium, 7 low, 2 info findings. Banking hard rules (no PAN/CVV, BigDecimal money, idempotency on financial POST, audit-via-outbox, account masking, sealed-error-detail handler) are upheld. The 3 medium findings are deployment-environment-gated (stub posture: `SecurityConfig` + `AccountClientStub` + `STUB_CUSTOMER_ID`) — acceptable for an isolated v1 scaffold but NOT shippable beyond local dev machines without the 10 documented `must_fix_before_staging` items being closed.

## Key Findings

### Severity Distribution

| Severity | Count |
|---|---|
| Critical | 0 |
| High | 0 |
| Medium | 3 |
| Low | 7 |
| Info | 2 |
| **Total** | **12** |

### Top Findings — Medium + Selected Low

| ID | Severity | OWASP | CWE | File:Line | Description | Remediation (short) |
|---|---|---|---|---|---|---|
| S-01 | medium | A05:2021 Security Misconfiguration | CWE-1188 | `infrastructure/config/SecurityConfig.java:64` | JWT resource-server validation commented out; no `JwtDecoder` registered. `anyRequest().authenticated()` declared but filter chain effectively deny-all in prod absence of mocks. No fail-fast on missing `issuer-uri`. | `@ConditionalOnExpression` bean that throws `BeanInitializationException` when ENVIRONMENT in (staging, prod) and `issuer-uri` blank. Wire `oauth2ResourceServer(jwt)` unconditionally. |
| S-02 | medium | A01:2021 Broken Access Control | CWE-639 | `interfaces/rest/TransferController.java:58` | `STUB_CUSTOMER_ID` (`00000000-0000-0000-0000-000000000001`) hardcoded as both `initiatorUserId` and `initiatorCustomerId`. Combined with deferred JWT, IDOR — any caller can read any transfer created under the same stub principal. | Inject `@AuthenticationPrincipal Jwt`, extract `sub` + `customer_id`. Throw 401 if absent. Delete `STUB_CUSTOMER_ID`. Gate controller with `@Profile("!prod")`. ArchUnit test forbidding hardcoded UUID literals in controllers. |
| S-03 | medium | A05:2021 Security Misconfiguration | CWE-1188 | `infrastructure/client/AccountClientStub.java:22` | `@Component` with no profile/property guard; auto-wired in staging/prod. Always returns ACTIVE + 10M THB. Once SecurityConfig hardens, the saga executes real state transitions on fake account data — silently marks COMPLETED, publishes events. Money-safety hazard. | `@Profile("!prod & !staging")` AND `@ConditionalOnProperty("transfer.account-client.stub.enabled", havingValue="true", matchIfMissing=false)`. WARN at `ApplicationReadyEvent`. Startup-failure bean if env=staging/prod and no real `AccountClient`. |
| S-04 | low | A05:2021 Security Misconfiguration | CWE-258 | `resources/application.yml:12` | `${DB_PASSWORD:transfer_pass}` default in YAML; service silently starts with known credential if env var missing. Credential already in git history — must be treated as compromised. | Change to `${DB_PASSWORD}` with no default. Move local defaults to `application-local.yml`. CI secret-scan gate (gitleaks/trufflehog). README note: Vault/CSI mount required in K8s. |
| S-05 | low | A09:2021 Logging & Monitoring Failures | CWE-117 | `interfaces/rest/TransferController.java:90` | `MDC.put` not bounded by `try/finally MDC.remove`. Thread-pool reuse leaks `correlationId` and `idempotencyKeyPrefix` across requests — misleading correlation in audit logs. | `ServletFilter` that `MDC.clear()` in finally end of every request. Integration test that runs two requests on same thread asserts MDC empty between. |
| S-06 | low | A09:2021 Logging & Monitoring Failures | CWE-778 | `infrastructure/persistence/OutboxEventPublisher.java:73` | Outbox `headers` hardcoded `"{}"`. US-007 relay will lose `traceparent` and `correlationId` at Kafka boundary. Audit/SOC2/PCI-DSS Req. 10 weakness. | Build map with at least `{correlationId, traceparent, eventId}` and `objectMapper.writeValueAsString(...)`. Test asserting headers contain `correlationId`. |
| S-07 | low | A09:2021 Logging & Monitoring Failures | CWE-532 | `application/saga/TransferSaga.java:64` | `log.info("saga.started ... src={}", transfer.getSourceAccountId())` relies on `AccountId.toString` masking last-4. A future refactor (e.g. Lombok `@ToString` regenerated by IDE) would silently leak full UUID — PDPA/GDPR finding. | Either (a) make `AccountId.toString` final + JUnit test asserting masked output, or (b) explicit masking at call site `... src_suffix=...substring(28)`. Spotbugs/PMD rule prohibiting `UUID.toString` in log statements. |
| S-08 | low | A09:2021 Logging & Monitoring Failures | CWE-532 | `infrastructure/persistence/OutboxEventPublisher.java:89` | Outbox payload includes full source/destination account UUIDs published to Kafka and copied to all downstream consumers. PDPA-adjacent identifiers. (memo & balance correctly excluded.) | Document event schema as 'personal data — internal only'. Kafka topic ACLs restrict to authorized services (DevOps). Retention purge job for `transfer_outbox` where `dispatched=true && created_at < now() - 30d`. |
| S-09 | low | A05:2021 Security Misconfiguration | CWE-489 | `resources/application.yml:47` | Actuator exposes `prometheus` and `metrics`; SecurityConfig permits `/actuator/prometheus` without auth. No in-app guard (`management.server.port` not isolated). | `management.server.port: 9090` + `management.server.address: 127.0.0.1` in prod profile. K8s NetworkPolicy restricting port 9090 to Prometheus pod. Disable swagger-ui in prod profile. |
| S-10 | low | A04:2021 Insecure Design | CWE-770 | `interfaces/rest/TransferController.java:77` | `POST /api/v1/transfers` has no rate-limit / per-customer throttle / payload size guard. Idempotency table flood vector (TTL=24h). Account-UUID enumeration possible (low yield). | Bucket4j or Resilience4j `@RateLimiter(name='transferCreate')` at 10 req/sec per JWT sub. `@PreAuthorize("hasAuthority(SCOPE_transfer.write)")`. Document rate-limit in OpenAPI `X-RateLimit-Remaining` headers. Confirm TTL purge job scheduled. |
| S-11 | info | A02:2021 Cryptographic Failures | CWE-916 | `application/usecase/CreateTransferUseCase.java:203` | SHA-256 (unsalted, no HMAC) used to hash Idempotency-Key. Acceptable because key is client-generated UUID v4 used as index, not authenticator. Concern: future maintainer might repurpose for authentication. | Document at call site: `// keyHash is an index, NOT an authenticator`. If reused for tenant-isolating capability, switch to HMAC-SHA-256 with per-env secret (Vault-rotated). |
| S-12 | info | A09:2021 Logging & Monitoring Failures | CWE-778 | `interfaces/rest/ProblemDetailExceptionHandler.java:79` | `ProblemDetail` reads `MDC.get("traceId")` but nothing populates `MDC.traceId` (only `correlationId` + `idempotencyKeyPrefix` set). `traceId` always null in error responses — auditor cannot link customer-visible error to server span. | Rename to `MDC.get("correlationId")` (works immediately), or add Micrometer-tracing baggage + W3C `traceparent` parser `ServletFilter`. Integration test asserting response `traceId` equals inbound `traceparent`. |

### Banking Hard Rules Status

| Rule | Pass | Notes |
|---|---|---|
| no_secrets_in_code | ❌ | `DB_PASSWORD` default `'transfer_pass'` in `application.yml` — Finding S-04 (low severity). |
| no_pii_in_logs | ✅ | `AccountId.toString` masks to last 4 chars; balances never logged; memo not in outbox payload. |
| no_float_for_money | ✅ | `Money` uses `BigDecimal` + `NUMERIC(19,4)` (Money.java + V001 schema). |
| idempotency_on_financial_post | ✅ | SHA-256 keyHash + 24h TTL + 409 on conflict (ADR-013). |
| audit_for_state_changes | ✅ | Outbox row written same-tx as business write. |
| no_pan_or_cvv | ✅ | No card data anywhere — verified by grep. |
| no_jwt_in_logs | ✅ | JWT never logged. |
| mass_assignment_safe | ✅ | `TransferRequest` is a record with explicit `@Valid` fields; entity setters package-private to adapter only. |
| tls_enforced | ⚠️ n/a | Deployment-config — DevOps responsibility. |
| jwt_rs256 | ⚠️ deferred | US-006; SecurityConfig is permit-all in v1 — Finding S-01. |
| rate_limit_on_auth | ⚠️ deferred | No auth endpoint in this service; transfer-endpoint rate-limit in Finding S-10. |

### STRIDE Threat Model

#### `POST /api/v1/transfers`

| Threat | Coverage |
|---|---|
| **S**poofing | ⚠️ Deferred to US-006 (JWT RS256). v1 uses hardcoded `STUB_CUSTOMER_ID` — must NOT ship beyond local dev. (S-02) |
| **T**ampering | ✅ BigDecimal + NUMERIC(19,4) + `@Version` optimistic lock + UNIQUE `reference_number`. Idempotency checksum (SHA-256 of canonical payload) detects replay-with-mutation → 409. |
| **R**epudiation | ✅ partial — Outbox rows `TransferRequested/Completed/Failed` in same tx; `correlation_id` persisted. Signed-event payload deferred to US-007 relay. |
| **I**nformation Disclosure | ✅ AccountId masked; balances never logged; ProblemDetail no stack traces; memo not in outbox. Concern: S-05 MDC leak. |
| **D**enial of Service | ⚠️ No rate-limit in v1 (S-10). Resilience4j present for accountClient but not on controller. Idempotency table TTL purge job not yet implemented. |
| **E**levation of Privilege | ⚠️ `@PreAuthorize("isAuthenticated()")` only — no claim-scope check. Must add `hasAuthority('SCOPE_transfer.write')` before US-006. |

#### `GET /api/v1/transfers/{transferId}`

| Threat | Coverage |
|---|---|
| **S**poofing | ⚠️ Same as POST — JWT validation deferred. |
| **T**ampering | ✅ Read-only endpoint. |
| **R**epudiation | ✅ GET logged at INFO with `correlation_id`. |
| **I**nformation Disclosure | ✅ Ownership check (use-case line 194) throws `TransferNotFoundException` (404) — does not leak resource existence. Pass-with-condition — depends on JWT subject correctly extracted (currently NOT — S-02). |
| **D**enial of Service | ✅ No rate-limit but UUID path param (~2^128) prevents enumeration. |
| **E**levation of Privilege | ⚠️ Same as POST — needs scope/role check before US-006. |

### OWASP Top 10 Coverage

| Category | Status | Notes / Findings |
|---|---|---|
| A01: Broken Access Control | ⚠️ | S-02 IDOR via stub principal — deployment-gated. |
| A02: Cryptographic Failures | ✅ info | S-11 SHA-256 idempotency (acceptable; informational). |
| A03: Injection | ✅ | JPA parameterized via Spring Data + `@Query` named params, no `String.format` on SQL. |
| A04: Insecure Design | ⚠️ | S-10 no rate-limit on financial endpoint. |
| A05: Security Misconfiguration | ⚠️ | S-01 (JWT off), S-03 (stub no profile), S-04 (DB default), S-09 (actuator exposed). |
| A06: Vulnerable & Outdated Components | ⚠️ | SCA not_run — must wire Dependabot/OWASP-DependencyCheck (ITEM-10). |
| A07: Identification & Authn Failures | ⚠️ | Deferred — JWT validation absent (S-01). |
| A08: Software & Data Integrity Failures | ✅ | Outbox-in-tx + UNIQUE constraints + `@Version` optimistic lock. |
| A09: Security Logging & Monitoring | ⚠️ | S-05 MDC leak, S-06 empty Kafka headers, S-07 fragile log mask, S-08 outbox PII governance, S-12 ProblemDetail traceId null. |
| A10: Server-Side Request Forgery | ✅ | No outbound HTTP from arbitrary input; AccountClient targets configured base-URL only. |

### Compliance Status

#### PCI-DSS — OUT OF SCOPE

Service is OUT of PCI-DSS card-data scope. Handles intra-bank transfer references and account UUIDs only. No PAN, no CVV, no track data. Cardholder data flow is the responsibility of a separate card-tokenization service.

- ✅ Req-3 — no PAN/CVV/track data anywhere (grep-verified).
- ✅ Req-4 — no card data in transit; THB transfer payload contains only UUIDs and amount.
- ✅ Req-6.5.1 — injection — JPA parameterized via Spring Data + `@Query` named params.
- ✅ Req-6.5.5 — improper error handling — RFC 7807 ProblemDetail without stack traces in body.
- ✅ Req-8 — n/a — no user credentials handled in this service.

Service remains in scope for general bank IT-security regulations (BoT) and PDPA.

#### GDPR

- **PII handled:** true
- **Lawful basis:** contract performance
- **Retention:** transfers 7y (audit/legal); `transfer_idempotency` 24h TTL; `transfer_outbox` dispatched + 30d (TBD with compliance).
- **Masking verified:** ✅

#### PDPA (Thailand)

- **Applicable:** ✅
- **Consent management:** Out-of-scope for this service — handled by identity-service consent registry (PDPA §19). Operates under existing banking contract.
- **Data residency:** All data (PostgreSQL, Kafka, logs) MUST reside in Thailand. K8s cluster + DB cluster must be pinned to `ap-southeast-1` (Bangkok) or on-prem TH per BoT cross-border data rules. DevOps to enforce via K8s topology constraint.

### Scans Run

| Scan Type | Result |
|---|---|
| SAST | ⚠️ not_run |
| SCA | ⚠️ not_run |
| Secrets | ⚠️ 1 finding (DB_PASSWORD default in `application.yml` — Finding S-04) |
| Container | ⚠️ not_applicable_yet |

### v1 Scaffold Acceptances

1. ✅ `AccountClientStub` auto-wired with canned 10M THB ACTIVE response — acceptable ONLY because service is not deployed anywhere reachable (gating required, ITEM-1).
2. ✅ `SecurityConfig` with commented-out `oauth2ResourceServer` JWT validation — acceptable ONLY because no production traffic reaches this service (ITEM-2).
3. ✅ `STUB_CUSTOMER_ID` hardcoded in TransferController — acceptable ONLY in combination with the above two gates (ITEM-3).
4. ✅ `DB_PASSWORD` default `'transfer_pass'` in `application.yml` — acceptable for local dev convenience (ITEM-4).
5. ✅ No rate-limit on `POST /api/v1/transfers` — acceptable for v1 scaffold (ITEM-5).
6. ✅ `OutboxEventPublisher.headers` hardcoded `'{}'` — acceptable in v1 (no Kafka relay wired yet — US-007) (ITEM-6).
7. ✅ `MDC.put` without try/finally `MDC.clear` — acceptable in v1 (single-threaded tests, no thread reuse observed) (ITEM-7).
8. ✅ `ProblemDetail` traceId MDC key always null — acceptable in v1 (no real cross-service tracing yet) (ITEM-8).

### Must-Fix-Before-Staging

| # | Item | Severity / Gate | Phase to Fix |
|---|---|---|---|
| ITEM-1 | `AccountClientStub` gated with `@Profile("!prod & !staging")` + `@ConditionalOnProperty(...)` AND real `AccountClient` adapter (Feign/WebClient + Resilience4j) present. Startup-failure bean if env=staging/prod and no real adapter. | 🔴 CRITICAL-FOR-DEPLOY | US-006 (Finding S-03) |
| ITEM-2 | `SecurityConfig.oauth2ResourceServer(jwt)` uncommented AND `spring.security.oauth2.resourceserver.jwt.issuer-uri` from Vault. Startup-failure `@ConditionalOnExpression` if env=staging/prod and `issuer-uri` blank. | 🔴 CRITICAL-FOR-DEPLOY | US-006 (Finding S-01) |
| ITEM-3 | `STUB_CUSTOMER_ID` deleted from `TransferController`. Replace with `@AuthenticationPrincipal Jwt` and extract `sub` + `customer_id`. Throw 401 if absent. Lands atomically with ITEM-2. | 🔴 CRITICAL-FOR-DEPLOY | US-006 (Finding S-02) |
| ITEM-4 | `DB_PASSWORD` default removed from `application.yml`. Use `${DB_PASSWORD}` with no default. Move local defaults to `application-local.yml`. CI secret-scan gate. | 🟠 BEFORE-PROD | DevOps / cfg (Finding S-04) |
| ITEM-5 | Rate-limit on `POST /api/v1/transfers` (Bucket4j or Resilience4j `@RateLimiter` at 10 req/sec per JWT sub). `@PreAuthorize` scope check `hasAuthority(SCOPE_transfer.write)`. | 🟠 BEFORE-PROD | US-006 (Finding S-10) |
| ITEM-6 | `OutboxEventPublisher` populates headers with at least `{correlationId, traceparent, eventId}`. Required for Kafka consumer correlation. | 🟡 BEFORE-US-007-RELAY-SHIPS | US-007 (Finding S-06) |
| ITEM-7 | `ServletFilter` that `MDC.clear()` in finally, or wrap controller bodies in try/finally. Integration test asserting MDC empty between requests on same thread. | 🟡 BEFORE-STAGING | QA / impl (Finding S-05) |
| ITEM-8 | Wire Micrometer-tracing + W3C `traceparent` `ServletFilter` populating `MDC.traceId` from inbound header. Or rename ProblemDetail `traceId` key to `correlationId` for v1. | 🟡 BEFORE-STAGING | impl (Finding S-12) |
| ITEM-9 | DevOps controls — actuator on internal management port 9090, K8s NetworkPolicy restricting Prometheus scrape, swagger-ui disabled in prod profile, K8s topology constraint pinning pods + DB to Thailand region (PDPA data residency). | 🟡 BEFORE-STAGING | DevOps (Finding S-09) |
| ITEM-10 | SCA scan (Dependabot / OWASP-DependencyCheck) wired in CI. SBOM published per build. Container scan (Trivy/Grype) on every image build. | 🟠 BEFORE-PROD | DevOps S8/S9 |

## Quality Gate Check

- [x] 0 critical findings
- [x] 0 high findings
- [x] Medium findings tracked with deployment gates
- [x] Banking hard rules verified (1 low-severity violation tracked)
- [x] STRIDE per endpoint complete
- [x] OWASP Top 10 coverage documented
- [x] PCI-DSS scope confirmed OUT
- [x] PDPA/GDPR posture documented (residency + retention follow-ups)
- [x] `must_fix_before_staging` enumerated with phase and gating advice
- [x] Coordinated with reviewer artifact S6 (stub-posture theme aligned)

## Security's Recommendation

**Approve to QA.** Implemented code passes all banking hard rules except `no_secrets_in_code` (low-severity YAML default). The medium findings are deployment-environment-gated by stub posture and acceptable only because the service is isolated. **DevOps MUST gate the 10 `must_fix_before_staging` items before any deploy beyond local dev** — particularly the 3 CRITICAL-FOR-DEPLOY items (ITEM-1, ITEM-2, ITEM-3) which must land atomically with US-006. QA should lock in functional + idempotency + security-positive tests now so US-006 hardening can be verified by regression rather than re-discovery.

## Action Items

For `banking-qa`:
1. Build functional test plan covering all happy-path + error scenarios for `POST` and `GET /api/v1/transfers`.
2. Lock idempotency coverage: same key + same payload → 200 replay; same key + different payload → 409; concurrent requests with same key → only one transfer created.
3. Add security-positive tests: ownership check (404 for non-owner GET), `@Valid` rejection (400 for malformed payload), ProblemDetail shape (RFC 7807 + no stack traces).
4. Verify masking: assert `AccountId.toString` returns masked last-4 in log capture tests.
5. Track `must_fix_before_staging` items 7 and 8 (MDC clear + traceId wiring) as QA-verifiable regressions.

## Links

- **Source JSON:** [S7-security-money-transfer.json](S7-security-money-transfer.json)
- **Previous artifact:** [S6-reviewer-money-transfer.md](S6-reviewer-money-transfer.md)
- **Next artifact:** [S8-qa-money-transfer.md](S8-qa-money-transfer.md) (Pending QA)
- **Communications timeline:** [../agents-comms/timeline.md](../agents-comms/timeline.md)
